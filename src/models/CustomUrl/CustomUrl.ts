export class CustomUrl {
    constructor(
        public readonly customUrlId: string,
        public readonly uid: string,
        public readonly urlString: string,
        public readonly textString: string
    ) {
    }
}